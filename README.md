# 数据加密与密码管理系统

一个安全的本地数据加密和密码管理工具，使用军事级加密标准保护您的敏感信息。

## 功能特性

- 🔐 **强加密算法**：使用 AES-256-GCM 加密标准
- 🔑 **密码管理**：安全存储和管理多个账户密码
- 📁 **文件加密**：加密/解密任意文件和文本数据
- 🛡️ **密钥派生**：使用 Argon2 密钥派生函数
- 💾 **本地存储**：所有数据加密后存储在本地，绝不上传云端

## 安全说明

- 主密码是访问所有数据的唯一凭证，请妥善保管
- 忘记主密码将无法恢复数据
- 加密数据存储在 `vault/` 目录
- 建议定期备份 `vault/` 目录

## 安装

```bash
pip install -r requirements.txt
```

## 使用方法

### Web界面版本（推荐）✨
```bash
python web_gui.py
```
然后在浏览器中打开：http://127.0.0.1:5000

### 命令行版本
```bash
python main.py
```

### 首次使用
1. 设置主密码（需要输入两次确认）
2. 主密码将用于保护所有加密数据

### 密码管理功能
- 添加新密码
- 查看已保存的密码
- 删除密码
- 复制密码到剪贴板

### 文件/数据加密
- 加密文本数据
- 解密已加密的数据
- 加密文件
- 解密文件

## 项目结构

```
.
├── main.py              # 命令行主程序入口
├── web_gui.py           # Web界面程序入口（推荐）
├── gui_app.py           # 桌面GUI程序（需要新版macOS）
├── crypto_manager.py    # 加密核心模块
├── password_vault.py    # 密码管理模块
├── file_encryptor.py    # 文件加密模块
├── utils.py             # 工具函数
├── templates/           # Web界面模板
├── vault/               # 加密数据存储目录
└── requirements.txt     # 依赖包
## Web界面功能特性

基于Flask的现代化Web界面，兼容性更好：

- 🌐 **浏览器访问**：无需安装额外软件，用浏览器即可使用
- 📋 **标签页界面**：密码管理、文件加密、文本加密分页显示
- 📁 **文件上传/下载**：拖放上传文件，一键下载加密/解密结果
- 👁️ **密码管理**：
  - 添加、查看、复制、删除密码
  - 自动生成强密码（可调长度）
  - 密码列表可视化
- 🔒 **文件加密/解密**：
  - 上传文件自动加密/解密
  - 自动下载处理后的文件
  - 支持大文件（最大100MB）
- 📝 **文本加密/解密**：
  - 直接在界面中输入文本
  - 一键复制加密/解密结果
- ⚙️ **设置管理**：在界面中直接更改主密码
- 🎨 **现代化设计**：响应式布局，支持移动设备
  - 一键复制加密/解密结果
- ⚙️ **设置管理**：在界面中直接更改主密码

## 技术细节

- **加密算法**：AES-256-GCM（认证加密）
- **密钥派生**：Argon2（内存难度型，抗ASIC攻击）
- **盐值**：每次加密使用随机盐值
- **IV/Nonce**：每次加密生成新的初始化向量

## 注意事项

⚠️ **重要提示**：
- 这是一个本地加密工具，数据安全性取决于主密码强度
- 建议使用至少12位包含大小写字母、数字和符号的强密码
- 定期备份 `vault/` 目录到安全位置
- 不要将主密码告诉任何人
